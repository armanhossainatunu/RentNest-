model Profile {
    id String @id @default(uuid())
    profileProto String?
    bio String?
    userId String @unique
    user User @relation(fields: [userId], references: [id])
    @@map("profiles")
}
